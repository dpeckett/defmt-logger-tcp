[package]
name = "defmt-logger-tcp"
version = "0.2.1"
edition = "2021"
license = "BSD-2-Clause"
description = "A defmt logger that serves logs over TCP."

[dependencies]
defmt = "0.3"

[features]
default = ["std"]
std = []